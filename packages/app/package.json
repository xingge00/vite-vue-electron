{
  "name": "vite-vue-electron-demo-app",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "vite:dev": "vite",
    "build:hot": "pnpm run clean && vue-tsc --noEmit && vite build --watch",
    "build:vite": "pnpm run clean && vue-tsc --noEmit && vite build",
    "clean": "rm -rf ./dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.37",
    "axios": "^0.23.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.17",
    "@vitejs/plugin-vue": "^3.1.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0",
    "vue-tsc": "^0.40.4"
  }
}